<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.CustomTableBaseDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.renren.modules.sys.entity.CustomTableBaseEntity" id="customTableBaseMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
    </resultMap>
    <select id="selectCustomFieldPage" resultType="Map" parameterType="java.util.List" >
        select
        defense.id,defense.user_id as userId,extend.*
        from custom_table_base defense
        left join (
        select
        <foreach collection="extendFields" item="item">
            max(case field_id when ${item.id} then `value` else '' end) as `${item.id}`,
        </foreach>
        defense_id
        from
        custom_extend
        group by defense_id) extend on (defense.id = extend.defense_id)

        order by  defense.id asc
    </select>

</mapper>